/**
 * Placeholder for future Google Drive metadata ingestion (files.readonly / drive.metadata.readonly).
 * Wire a fetch + normalizer here and call from refresh-signals when product adds OAuth scopes.
 */
export type DriveIngestionStub = 'not_configured';
